node_modules/
dist/
.astro/
.DS_Store
npm-debug.log*
*.zip

# local archive of the old Hugo site + micro.blog exports — never commit
archive/

# Coolify API token for LAN deploys — never commit
.env.deploy
